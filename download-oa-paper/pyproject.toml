[project]
name = "download-oa-paper"
version = "0.1.0"
description = "Download PMC paper PDFs and supplementary files from PMID input"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[project.scripts]
download-oa-paper = "main:main"
